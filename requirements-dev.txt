# Install with: pip install -r requirements-dev.txt
# (requirements.txt must already be installed - this only adds test tooling)

-r requirements.txt

pytest==8.3.4
httpx==0.28.1        # required by FastAPI's TestClient
